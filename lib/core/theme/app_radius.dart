import 'package:flutter/material.dart';

abstract final class AppRadius {
  static const BorderRadius large = BorderRadius.all(Radius.circular(20));

  static const BorderRadius card = BorderRadius.all(Radius.circular(18));

  static const BorderRadius medium = BorderRadius.all(Radius.circular(16));

  static const BorderRadius button = BorderRadius.all(Radius.circular(12));

  static const BorderRadius progress = BorderRadius.all(Radius.circular(10));
}
